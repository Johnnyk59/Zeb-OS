#
# docker-compose.windows.yml — Windows Docker Desktop compatible
#
# Differences from docker-compose.yml:
#   - Removes `network_mode: host` (not supported on Docker Desktop for Windows)
#   - Uses explicit port mappings instead
#   - Uses Windows-style volume path for ~/.zeb
#
# Usage:
#   docker compose -f docker-compose.windows.yml up -d
#
services:
  gateway:
    image: nousresearch/zeb-agent:latest
    container_name: zeb
    restart: unless-stopped
    volumes:
      - ${USERPROFILE}/.zeb:/opt/data
    environment:
      - ZEB_UID=10000
      - ZEB_GID=10000
    command: ["gateway", "run"]

  dashboard:
    image: nousresearch/zeb-agent:latest
    container_name: zeb-dashboard
    restart: unless-stopped
    depends_on:
      - gateway
    volumes:
      - ${USERPROFILE}/.zeb:/opt/data
    environment:
      - ZEB_UID=10000
      - ZEB_GID=10000
      - ZEB_DASHBOARD_HOST=0.0.0.0
    ports:
      - "127.0.0.1:9119:9119"
    command: ["dashboard", "--host", "0.0.0.0", "--port", "9119", "--no-open", "--insecure"]
